import 'package:flutter/material.dart';
import 'package:proyecto_final/src/tools/functions_generics.dart';

class DrawerWidget extends StatelessWidget {
  @override
  Widget build(
    BuildContext context,
  ) {
    return menu(context);
  }
}
